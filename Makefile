# Specific Makefile for the project {{PROJECT_NAME}}
include Makefile.common.mak

# EXT_REPO_NAME := repo_name
# EXT_BRANCH := main
# EXT_VERSION := 0.1.0

# fetch-all:  ## Fetch all dependencies
# 	@$(MAKE) fetch-wheel REPO=$(EXT_REPO_NAME) BRANCH=$(EXT_BRANCH) VERSION=$(EXT_VERSION)